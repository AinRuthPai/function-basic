// 매개변수와 인자 (Parameter and Argument)

// 매개변수 : 함수의 특별한 성질을 나타내는 변수
// 인자 : 함수에 input으로 들어온 데이터

// 괄호 안에 age가 매개변수
function getAge(age) {
  return age + 살;
}

// 괄호 안에 90이 인자
let result = getAge(90);
console.log(result);
